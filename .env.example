# Copy to .env and fill in values
# NOTE: Do not commit your real tokens.

POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=merge_db

# Optional (recommended for higher rate limits)
QIITA_ACCESS_TOKEN=
DEV_ACCESS_TOKEN=
